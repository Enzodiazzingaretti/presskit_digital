{
  "name": "kexxy-presskit",
  "version": "1.0.0",
  "description": "KEXXY - Techno Artist Press Kit",
  "scripts": {
    "dev": "npx serve -l 8080 -s .",
    "start": "npx serve -l 8080 -s .",
    "build": "if (!(Test-Path public)) { New-Item -ItemType Directory -Name public }; Copy-Item *.html, *.css, *.js, *.webp, *.png, *.xml public/ -ErrorAction SilentlyContinue; echo 'done'",
    "preview": "npx serve -l 8080 -s ."
  },
  "keywords": [
    "techno",
    "dj",
    "presskit"
  ],
  "author": "KEXXY",
  "license": "MIT",
  "devDependencies": {
    "serve": "^14.2.0"
  }
}
